{
    "name": "telemedbot-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for TeleMedBot",
    "scripts": {
        "test": "hardhat test",
        "compile": "hardhat compile",
        "deploy": "hardhat run scripts/deploy.ts"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.27.2",
        "ts-node": "^10.0.0",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.0"
    }
}
